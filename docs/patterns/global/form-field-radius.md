# Form field radius

- Single-line inputs (text, date, search) are **pill** shaped — `rounded-full`.
- Multi-line form components (textarea, rich-text editor, the teaser / select
  container) use a **20px** radius — `rounded-[20px]`.

Apply the same to any new form field. Reference: the editor Content view in
`src/pages/pages.tsx`.
